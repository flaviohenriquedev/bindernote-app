import {AppLayoutContainer} from "@/components/layout/app-layout/AppLayoutContainer";
import {AppLayoutHeader} from "@/components/layout/app-layout/AppLayoutHeader";
import {AppLayoutContent} from "@/components/layout/app-layout/AppLayoutContent";

export const AppLayout = {
    Container: AppLayoutContainer,
    Header: AppLayoutHeader,
    Content: AppLayoutContent
}
